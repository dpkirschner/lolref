<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<% @champions.each do |champion| %>
  <tr>
    <td><%= champion.name %> <%= champion.title %> <%= champion.id %></td>
  </tr>
<% end %>
</table>